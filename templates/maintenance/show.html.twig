{% extends 'base.html.twig' %}

{% block title %}Maintenance{% endblock %}

{% block body %}
    <h1>Maintenance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maintenance.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ maintenance.date ? maintenance.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Kilometrage</th>
                <td>{{ maintenance.kilometrage }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ maintenance.type }}</td>
            </tr>
            <tr>
                <th>Cout</th>
                <td>{{ maintenance.cout }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ maintenance.description }}</td>
            </tr>
            <tr>
                <th>Prochain_entretien</th>
                <td>{{ maintenance.prochainEntretien }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_maintenance_index') }}">back to list</a>

    <a href="{{ path('app_maintenance_edit', {'id': maintenance.id}) }}">edit</a>

    {{ include('maintenance/_delete_form.html.twig') }}
{% endblock %}
